const para = document.getElementById("para")! as HTMLInputElement;

para.value = "HELLO1";

// --------------------- 2 ----------------------------
// const para = document.getElementById('para');

// if (para) {
//   (para as HTMLInputElement).value = "HELLO2";
// }
