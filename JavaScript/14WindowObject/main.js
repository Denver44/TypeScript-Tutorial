// The window object is supported by all browsers.
// It represents the browser's window.
// All global JavaScript objects, functions, and variables automatically become members of the window object.
// Global variables are properties of the window object.
// Global functions are methods of the window object.

let a = window; // window is global object the window is the our browser  .

//  Alert
// window.alert("You have been Hacked");
// alert("Hacked");

//  PROMPT
// a = prompt("Enter your name");

a = window.innerHeight;
a = window.history;
a = window.toString();
a = window.scrollY;
a = window.scrollX;
// a = confirm("are u sure");

a = window.innerWidth;
console.log(a); // all function of window is printed on console.
