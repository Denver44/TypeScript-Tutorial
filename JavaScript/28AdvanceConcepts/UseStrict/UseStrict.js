// Duplicat paramter
// declaring a variable without let var and const
// it will throw error if we wrote "use strict" at the top.
"use strict";
sum(4, 5);

function sum(a, a) {
  c;
  console.log(a + b);
}
